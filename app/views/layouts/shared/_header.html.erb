<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "OPPCIS", root_path, :class=>"brand" %>
      <div class="nav-collapse collapse">
        <ul class="nav pull-right">
			<% if user_signed_in?%>
				<%= admins_only do%>
					<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-cog"></i>Settings</a>
					<ul class="dropdown-menu">
						<li><%= link_to "Users", admin_users_url %></li>
						<li><%= link_to "Agencies", agencies_url %></li>
					</ul>
					</li>	
				<% end %>
			<% end %>
			
			<% if !user_signed_in?%>
				<li><a href="javascript:{}" data-section="2" id="join-button">Register</a></li>
				<li><a href="javascript:{}" data-section="3" id="login-button">Login</a></li>
			<% else %>
				<li><a href="#" style="cursor:crosshair;">You are signed in as <u><%= current_user.username %></u></a></li>
				<li><%= link_to "Logout", destroy_user_session_url, method: :delete,:confirm=>"Are you sure you want to log out?" %></li>
	      	<% end %>
       </ul>
        <ul class="nav">
        	<% if user_signed_in? %>
	      		<% if current_user.admin? %>
	      		<% else %>
			        <li><%= link_to "PPMPs", project_procurement_management_plans_url %></li>
					<li><%= link_to "APPs", "#" %></li>
					<li><%= link_to "PMRs", "#" %></li>
					<li><%= link_to "CPMRs", "#" %></li>
					<li><%= link_to "APCPIs", "#" %></li>
		      	<% end %>
	    </ul>
			<% else %>
	      	<% end %>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>