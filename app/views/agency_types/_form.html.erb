<%= form_for(@agency_type) do |f| %>
  <% if @agency_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agency_type.errors.count, "error") %> prohibited this agency_type from being saved:</h2>

      <ul>
      <% @agency_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :rows=>5 %>
  </div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary" %>
  </div>
<% end %>
